rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas para la colección de leads de preventa
    match /preventa_leads/{leadId} {
      // Permitir lectura y escritura solo a usuarios autenticados del backend
      allow read, write: if request.auth != null && 
        request.auth.token.email == resource.data.email;
      
      // Permitir creación de nuevos leads (sin autenticación para el formulario)
      allow create: if true;
    }
    
    // Reglas más restrictivas para otras colecciones
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
